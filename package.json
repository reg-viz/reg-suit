{
  "private": true,
  "scripts": {
    "clean": "rimraf packages/**/lib/**",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint packages/*/src/**/*.ts packages/*/test/**/*.ts",
    "watch:interface": "tsc -w -p packages/reg-suit-interface/src/tsconfig.build.json",
    "watch:core": "tsc -w -p packages/reg-suit-core/src/tsconfig.build.json",
    "watch:cli": "tsc -w -p packages/reg-suit-cli/src/tsconfig.build.json",
    "watch:gitHashPlugin": "tsc -w -p packages/reg-keygen-git-hash-plugin/src/tsconfig.build.json",
    "watch:s3Plugin": "tsc -w -p packages/reg-publish-s3-plugin/src/tsconfig.build.json",
    "watch:slackPlugin": "tsc -w -p packages/reg-notify-slack-plugin/src/tsconfig.build.json",
    "watch": "run-p watch:*"
  },
  "devDependencies": {
    "@types/node": "^7.0.31",
    "lerna": "^2.0.0-rc.5",
    "npm-run-all": "^4.0.2",
    "rimraf": "^2.6.1",
    "tslint": "^5.4.3",
    "typescript": "^2.3.4"
  },
  "dependencies": {
    "glob": "^7.1.2",
    "tslint-eslint-rules": "^4.1.1"
  }
}
