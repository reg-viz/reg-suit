{
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:core": "tsc -w -p packages/reg-suit-core/src/tsconfig.json",
    "watch:s3plugin": "tsc -w -p packages/reg-publish-s3-plugin/src/tsconfig.json",
    "watch": "run-p watch:*",
    "clean": "rimraf packages/**/lib/**"
  },
  "devDependencies": {
    "@types/node": "^7.0.31",
    "lerna": "^2.0.0-rc.5",
    "npm-run-all": "^4.0.2",
    "rimraf": "^2.6.1",
    "typescript": "^2.3.4"
  }
}
